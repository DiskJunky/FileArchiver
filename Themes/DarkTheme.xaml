<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Dark Theme Colors -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="ControlBackgroundBrush" Color="#2D2D30"/>
    <SolidColorBrush x:Key="TextForegroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#3F3F46"/>
    <SolidColorBrush x:Key="GroupBoxHeaderBrush" Color="#CCCCCC"/>
    <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#252526"/>
    <SolidColorBrush x:Key="ListBackgroundBrush" Color="#2D2D30"/>
    <SolidColorBrush x:Key="ListAlternateBackgroundBrush" Color="#323232"/>
    <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="#3F3F46"/>
    <SolidColorBrush x:Key="ButtonHoverBrush" Color="#4A4A50"/>
    <SolidColorBrush x:Key="ButtonPressedBrush" Color="#555555"/>
    <SolidColorBrush x:Key="DisabledForegroundBrush" Color="#666666"/>
    <SolidColorBrush x:Key="DisabledBackgroundBrush" Color="#2D2D30"/>
    <SolidColorBrush x:Key="SplitterBrush" Color="#3F3F46"/>
    <SolidColorBrush x:Key="ProgressBarBrush" Color="#4CAF50"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="#6FCF97"/>
    <SolidColorBrush x:Key="WarningBrush" Color="#FFA726"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="#EF5350"/>
    <SolidColorBrush x:Key="ArchiveButtonBrush" Color="#4CAF50"/>
    <SolidColorBrush x:Key="ArchiveButtonHoverBrush" Color="#45A049"/>

    <!-- Text Colors -->
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="#AAAAAA"/>
    <SolidColorBrush x:Key="ConsoleTextBrush" Color="#D4D4D4"/>

    <!-- GroupBox Style -->
    <Style TargetType="GroupBox">
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextForegroundBrush}"/>
    </Style>

    <!-- TextBox Style -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="CaretBrush" Value="{StaticResource TextForegroundBrush}"/>
    </Style>

    <!-- Button Style -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ButtonBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{StaticResource DisabledBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource DisabledForegroundBrush}"/>
                <Setter Property="BorderBrush" Value="#1A1A1E"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ListView Style -->
    <Style TargetType="ListView">
        <Setter Property="Background" Value="{StaticResource ListBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
    </Style>

    <!-- ListBox Style -->
    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ConsoleTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
    </Style>

    <!-- TextBlock Style -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextForegroundBrush}"/>
    </Style>

    <!-- MenuItem Style for Dark Theme -->
    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#3F3F46"/>
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="#3F3F46"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- GridViewColumnHeader Style for Dark Theme -->
    <Style TargetType="GridViewColumnHeader">
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="Padding" Value="5,3"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
    </Style>
</ResourceDictionary>